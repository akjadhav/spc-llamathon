{
  "name": "spc-llamathon",
  "version": "1.0.0",
  "description": "Python env cd llamathon-env/bin && source activate && cd ../..",
  "main": "parser.js",
  "scripts": {
    "test": "jest"
  },
  "author": "Alexander Waitz",
  "dependencies": {
    "esprima": "^4.0.1"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
